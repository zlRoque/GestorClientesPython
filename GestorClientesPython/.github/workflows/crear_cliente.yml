name: Creación de Cliente

on:
  workflow_dispatch:  # Permite ejecutarlo manualmente

jobs:
  notificar_creacion:
    runs-on: ubuntu-latest
    steps:
      - name: Notificación de nuevo cliente
        run: echo "🆕 Se ha creado un nuevo cliente en la aplicación. Todos los miembros han sido notificados."
